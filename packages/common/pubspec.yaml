name: common
description: The base package with common functionality that is not dependant on Flutter.
version: 0.5.1
publish_to: "none"

environment:
  sdk: ">=2.15.0 <3.0.0"

dependencies:
  http: ^0.13.5
  logger: ^1.1.0

