name: subscription_repository
description: A repository to subscribe to a BlitzAPI SSE endpoint.
version: 0.5.1
publish_to: "none"

environment:
  sdk: ">=2.15.0 <3.0.0"

dependencies:
  http: ^0.13.5
 
  # locals
  common: ^0.5.1
  sse_client: ^0.5.1

