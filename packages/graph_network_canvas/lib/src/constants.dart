import 'package:flutter/material.dart';

const defaultNodeSize = Size(250.0, 250.0);
const defaultNodeMarginAll = 8.0;
const defaultNodeElevation = 5.0;
const defaultNodeElevationDragged = 15.0;
const defaultSocketSize = Size(15.0, 15.0);
