name: blitz_gui_workspace

environment:
  sdk: '>=2.18.0 <3.0.0'

dev_dependencies:
  melos: ^3.0.1
  import_sorter:
    git: https://github.com/fusion44/import_sorter

import_sorter:
  comments: false
  additional-paths:
    - apps
    - packages